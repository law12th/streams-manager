package com.law12th.streamsmanager.constants

object Constants {
    /** in-match statistics */
    const val IMS = "IMS"
    /** pre-match statistics */
    const val PRMS = "PRMS"
    /** post-match statistics */
    const val POMS = "POMS"
    /** match day */
    const val MD = "MD"
    /** in-match betting odds */
    const val IBO = "IBO"
    /** pre-match betting odds */
    const val PBO = "PBO"

    const val EveryAfterFiveMinCronExpression = "0 0/5 * * * ?"

    const val EveryAfterThreeMinCronExpression = "0 0/3 * * * ?"

    const val EveryAfterTwoMinCronExpression = "0 0/2 * * * ?"

    const val EveryAfterOneMinCronExpression = "0 0/1 * * * ?"

    const val EveryAfterOneDayCronExpression = ""

    const val Timezone = "Africa/Lusaka"
}